#!/usr/bin/perl
use utf8;
use strict;
use warnings;
use Test::Simple tests => 1;

# use SmallDatabase.pm;

# my $expected = 10;

# SMALL_DATABASE->SET('gg', $expected);
# my $got = SMALL_DATABASE->GET('gg');

# ok( $got eq $expected, 'new() works' );
